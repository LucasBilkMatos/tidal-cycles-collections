speed = 0.5
shape(20,0.5,0.9)
.color(0.9,0.9,50)
  .scale(() => a.fft[1]*0.5)
  .repeat(() => a.fft[2]*2)
  .modulateRotate(o0, () => a.fft[0]*3)
  .scale(() => a.fft[1]*0.5)
  .modulate(noise(2,2))
  .rotate(1, .2)
  //.invert(2.4)
.out(o0)
